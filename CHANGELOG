## Version 8.4.13

- ability to change labels and hide fields in ALKIS templates

## Version 8.4.12

- server configuration fixes

## Version 8.4.11

- minor ALKIS templates improvements

## Version 8.4.10

- minor bugs fixes and improvements

## Version 8.4.9

- added the auto unit `u` for lengths and areas in geometry format placeholders
- added support for the OSIRIS format in the feature info parser
- improved HTML escaping in ALKIS templates
- the delete button in the edit sidebar respects the model delete permission

## Version 8.4.8

- added the `xFrameOptions` site option to configure the `X-Frame-Options` header
- config files with the `.yaml` extension are now recognized
- improved error reporting for layer tree configuration

## Version 8.4.7

- added transient sessions for stateless authentication methods
- specific error types and status codes for failed HTTP requests
- improved application home page templates and styles
- fixed resource leaks in the server monitor
- fixed URL generation for URLs with a scheme but without a host

## Version 8.4.6

- improved SVG sanitization
- stricter validation of redirect URLs in the client
- added options for authentication throttling
- hardened the file authentication provider against user-enumeration timing attacks
- image and print requests are now validated against pixel size limits
- added an absolute session lifetime option `maxLifeTime`
- added `proxyCount` for reliable client-IP detection behind proxies
- deprecated `web.sites` in favor of a single `web.site`
- added `site.hostnames`, deprecated `site.host`
- improved preview generation for file fields

## Version 8.4.5

- added default templates for application and project home pages
- added default rewrite rules to route requests to the new home page templates
- added new action `webPage` to serve the new templates
- deprecated the `rewrite` config option in favor of `rewriteRules`
- `assets` defaults to `/data/assets` if that directory exists, otherwise to null
- deprecated the `index` and `project` assets in favor of the `application.home` and `project.home` templates
- deprecated the `errorPage` option in favor of the `application.error` template

## Version 8.4.4

- added Postgres storage provider

## Version 8.4.3

- CSV exporter
- optimized GDAL-related vector processing

## Version 8.4.2

- shapefile, KML and GML exporters
- update SSL configuration to the Mozilla intermediate profile

## Version 8.4.1

- basic feature export framework
- configuration now accepts multiple comma-separated paths
- improved config error reporting 

## Version 8.4.0

- added functions to scale GDAL rasters
- fixed CRS transformation issues for certain projections
- fixed concurrency issues with temporary files

projects+ {
    title "QGIS layer"
    metadata.abstract "`qgis` layers display complete QGIS Projects as WebSuite layers, keeping the structure"

    map {
        center {DEMO_DUESSELDORF_3857}
        zoom.initScale 30000
        zoom.maxScale  1000000
    }

    map.layers+ {
        title "Düsseldorf"
        type "qgis"
        path "poi.qgs"
    }

    @demo_osm_background
}
